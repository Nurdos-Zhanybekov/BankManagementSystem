package io.project.model;

public class BankWorker extends User {
    public BankWorker(int id, String account_type, String login, String password) {
        super(id, account_type, login, password);
    }
}
